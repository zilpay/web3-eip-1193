{
  "name": "web3-evm",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun build ./index.ts --outdir ./dist --target browser --format iife --no-minify && bun run test/server.ts",
    "build": "bun build index.ts --outdir ./dist --target browser --minify --sourcemap=none --format iife"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
