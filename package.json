{
  "name": "web3-evm",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun build ./index.ts --outdir ./dist --target browser && bun run test/server.ts",
    "build": "bun build index.ts --outdir dist --target browser --minify --sourcemap=none"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
